This script creates a local subversion repository from the TeX Live git mirror.

Copying every revision from the git repo would be too slow and require too much
disk space, so only the revisions and tags mapped in the script are copied over.
